{
  "name": "react-app-deployer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "cd client && npm install && npm run build",
    "build": "serve -s client/build"
  },
  "dependencies": {
    "serve": "^12.0.1"
  }
}
